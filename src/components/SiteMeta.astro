---
/**
 * SiteMeta Component
 * SEO-ready component for page titles, descriptions, and Open Graph tags
 */

interface Props {
  title: string;
  description?: string;
  image?: string;
  canonicalUrl?: string;
  type?: 'website' | 'article' | 'profile';
  author?: string;
  publishedTime?: string;
  modifiedTime?: string;
  keywords?: string[];
  noindex?: boolean;
  nofollow?: boolean;
  twitterCard?: 'summary' | 'summary_large_image' | 'player' | 'app';
  twitterCreator?: string;
}

const {
  title,
  description = 'A portfolio built with Neo-Brutalism and comic-book aesthetics',
  image = '/og-image.png',
  canonicalUrl = Astro.url.href,
  type = 'website',
  author,
  publishedTime,
  modifiedTime,
  keywords = [],
  noindex = false,
  nofollow = false,
  twitterCard = 'summary_large_image',
  twitterCreator,
} = Astro.props;

const siteName = 'Portfolio Comic Brutalism';
const fullTitle = `${title} | ${siteName}`;
const ogImage = image.startsWith('http') ? image : new URL(image, Astro.site).href;
const robots = `${noindex ? 'noindex' : 'index'}, ${nofollow ? 'nofollow' : 'follow'}`;
---

<!-- Primary Meta Tags -->
<title>{fullTitle}</title>
<meta name="title" content={fullTitle} />
<meta name="description" content={description} />
<meta name="robots" content={robots} />
{keywords.length > 0 && <meta name="keywords" content={keywords.join(', ')} />}
{author && <meta name="author" content={author} />}
<link rel="canonical" href={canonicalUrl} />

<!-- Open Graph / Facebook -->
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImage} />
<meta property="og:image:alt" content={description} />
<meta property="og:site_name" content={siteName} />
<meta property="og:locale" content="en_US" />

{type === 'article' && publishedTime && (
  <meta property="article:published_time" content={publishedTime} />
)}
{type === 'article' && modifiedTime && (
  <meta property="article:modified_time" content={modifiedTime} />
)}
{type === 'article' && author && (
  <meta property="article:author" content={author} />
)}

<!-- Twitter -->
<meta name="twitter:card" content={twitterCard} />
<meta name="twitter:url" content={canonicalUrl} />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImage} />
{twitterCreator && <meta name="twitter:creator" content={twitterCreator} />}

<!-- Additional SEO -->
<meta name="format-detection" content="telephone=no" />
<meta name="theme-color" content="#ea2a33" />
<meta name="color-scheme" content="light" />

<!-- JSON-LD Structured Data -->
<script type="application/ld+json" set:html={JSON.stringify({
  "@context": "https://schema.org",
  "@type": "ProfessionalService",
  "name": "Samuel Kaunang Web Development",
  "alternateName": "Saka.Dev",
  "description": "Jasa Pembuatan Website Profesional dan Aplikasi Bisnis untuk UMKM Indonesia. Spesialis Scalable Backend & Modern Frontend.",
  "priceRange": "Rp$$",
  "url": canonicalUrl,
  "image": ogImage,
  "jobTitle": "Full-stack Developer",
  "knowsAbout": [
    "Backend Development",
    "System Architecture", 
    "Spring Boot",
    "React.js",
    "Next.js",
    "Java",
    "TypeScript",
    "Product Management",
    "Digital Business Strategy"
  ],
  "sameAs": [
    "https://github.com/SamuelKaunang",
    "https://www.linkedin.com/in/samuel-kaunang-948246286",
    "https://www.instagram.com/samuel.knng"
  ],
  "email": "samuelymkaunang@gmail.com",
  "telephone": "+6282190599569",
  "address": {
    "@type": "PostalAddress",
    "addressCountry": "ID"
  }
})} />

